import { ReactNode } from "react";

interface Props {
    children: ReactNode,
    href: string,
    addClassName?: string,
}

const ContentContainer = ({  }: Props) => {
    return (
        <div>

        </div>
    )
}

export default ContentContainer;

